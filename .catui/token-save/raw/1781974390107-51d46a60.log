17:import {logEvent} from '../debug/logBus';
19:import {ABNORMAL_POSTURES} from './utils';
39:    if (!ABNORMAL_POSTURES.includes(s.posture)) {
52:    logEvent('model', '端侧模型推理开始');
53:    logEvent('infer', `输入 → ${prompt.replace(/\n/g, ' ')}`);
62:          logEvent('infer', `输出 ← ${acc.trim()}`);
69:        logEvent('model', '端侧推理失败/不可用 → 规则兜底');
91:        if (ABNORMAL_POSTURES.includes(s.posture) && Date.now() - lastTriggerTs >= HOLD_REPEAT_MS) {
